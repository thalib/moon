### Documentation

View HTML documentation in your browser: [http://localhost:6006/doc/](http://localhost:6006/doc/)

Get Markdown documentation (for humans and AI coding agents) via curl:

```bash
curl "http://localhost:6006/doc/llms.md"
```

Or view in browser: [http://localhost:6006/doc/llms.md](http://localhost:6006/doc/llms.md)

For text format, use:

```bash
curl "http://localhost:6006/doc/llms.txt"
```

Get JSON appendix for machine consumption:

```bash
curl "http://localhost:6006/doc/llms.json" | jq .
```

Refresh documentation cache:

```bash
curl -X POST "http://localhost:6006/doc:refresh" \
  -H "Authorization: Bearer $ACCESS_TOKEN" | jq .
```

**Response (200 OK):**

```json
{
  "message": "Documentation cache refreshed successfully"
}
```
